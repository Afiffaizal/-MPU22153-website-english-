# Echo Memory Recall

`Recall -Query "<question or keywords>"` searches the logged-in user's local profile and selected agent files. In team mode it also searches all unlocked shared project events, including records authored by other members.

The helper returns up to 20 keyword matches with source labels. A match is evidence to inspect, not proof that an interpretation is correct. See [recall-format.md](recall-format.md).
