# Recall response

Answer from records returned by `Recall`. Cite a source label such as `agent.diary/<timestamp>` or `agent.topics/<topic-id>/<timestamp>` when a claim depends on a record.

If there is no match, say that no supporting record was found in the available local and shared project history. Do not inspect another user's local cache files.
