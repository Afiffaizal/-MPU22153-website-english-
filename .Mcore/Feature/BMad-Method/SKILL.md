---
name: bmad-method
description: Route software work through the MemoryCore Direct, Build, Epic, or Project workflow when a request needs planning, implementation, or review.
---

# BMad Method in MemoryCore

This is a MemoryCore adaptation of [BMad Method](https://github.com/bmad-code-org/bmad-method). The operational workflow lives in `../Work-Flow/SKILL.md`; read and follow it. Use the smallest path that fits the work, preserve acceptance checks and decisions with MemoryCore helper actions, and call `RecordTurn` after each user instruction.

Run this native workflow through the current AI with the existing MemoryCore helper. No BMad installer or extra account is needed. Use `.Mcore/skills-and-mcp.md` for routing and integration details. Do not create provider folders in this project.
